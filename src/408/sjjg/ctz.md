---
title: 数据结构错题集
icon: book
---
## 1.1 数据结构的基本概念
1. **以下关于数据结构的说法中，正确的是：**

错选：C. 数据的逻辑结构唯一决定其存储结构

正选：A. 数据的逻辑结构独立于其存储结构

解析：数据的逻辑结构是从面向实际问题的角度出发的，只采用抽象表达方式，独立于存储结构；数据的存储方式有多种不同的选择

## 1.2 算法与算法评价

P6

1. **一个算法应该是**

错选：C. 要满足五个基本特性

正选：B. 问题求解的步骤

解析：C是算法的必要条件，不能成为算法的定义

2. **最后一行语句的频度在最坏情况下是？**
```c++
for(i = n-1; i>1;i--)
 for(j=1;j<i;j++)
  if(A[j]>A[j+1])
    A[j]与A[j+1]对换
```


错选：B. O(nlogn)

正选：D. O(n^2^)

个人原因，记住等差数列的和(三角形)是O(n^2^)而不是O(nlogn)!!!!!

3. **以下算法中最后一句的执行次数为**
```c++
int m=0,i,j;
for(int i=1;i<=n;i++)
 for(j=1;j<=2*i;j++)
    m++;
```


错选：D. n^2^

正选：A. n(n+1)

哥们你猪鼻吧，等差数列求和忘得一干二净了

内层循环每次都执行了2i次，而外层循环为n次

即，从2i(i=1)加到2i(i=n)，那么就是...

首项为2，公差为2，末项为2n的等差数列求和

`(2+2n)n/2`=`n(n+1)`

4. **下列函数的时间复杂度是()**
```c++
int func(int n){
    int i=0 , sum=0;
    while(sum<n) sum += ++i;
    return i;
}d
```

错选： A. O(logn)

正选： B. O(n^1/2^)

还是等差数列，求得sum=(1+i)*i/2

那么，将i替换为循环次数t(执行了t次)，就有

$$\frac{(1 + t) \cdot t}{2} < n$$



所以时间复杂度为O(n^1/2^)

5. **下列程序段的时间复杂度是(重点)**
 ```c++
 int sum=0
 for(int i=1;i<n;i*=2)
    for(int j=0;j<i;i++)
        sum++;
 
 ```
 错选：C. O(nlogn)

 正选：B. O(n)

别被坑了，看起来是内外双重嵌套循环，实际上内循环可以直接数出来次数为i

那么，可以将这个一样转变为一个等比数列的求和问题

即：1+2+4+...+2^t^

其中，t为内层循环的执行次数，有

$$2^t<n\leq2^{t+1}$$

那么就是...

$$T_{\text{(总执行次数)}}=\frac{1 - 2^t}{1-2}=2^{t+1}-1$$

所以有

$$n-1 \leq T \text{且} T<2n-1$$

>我草爆了这里真的难受，之后再回来看吧

6. 以下程序段的算法时间复杂度是
```c++
for(i=0;i<n;i++)
    for(j=0;j<m;j++)
        a[i][j]=0;
```

错解：n^2^

正解：m·n

笨蛋。

## 2.2 线性表的顺序表示

1. **Q3 线性表的顺序存储结构是一种()**

错选：B. 顺序存取的存储结构

正解：A. 随机存取的存储结构

存取是指读写方式，顺序表支持随机访问，也就是随机存取了！

2. **Q9 设线性表中有n个元素，严格来说，以下操作中，()在顺序表上的实现要比在链表上实现的效率高**

I.输出第i个元素的值
II. 交换第3个元素与第4个元素的值
III. 顺序输出这n个元素的值

错选：A. I
正选：C. I、II

注意III实际上是同等效率。

II实际上在顺序表上只需要3次交换操作，但是链表效率较低

## 2.3 线性表的链式表示

1. **Q3 链式存储设计时，结点内的存储单元地址()**
   
错解：C. 不一定连续

正解：A. 一定连续

注意是结点内存储的单元地址，不是结点的地址，对于链表来说，结点内存储的单元地址一定是连续的(不然不叫链表了吧)


2. **Q7 给定有n个元素的一维数组，建立一个有序单链表的最低时间复杂度是()**

错解：B. O(n)

正解：D. O(nlog~2~n)

对数组先排序再插入需要花费O(log~2~n)·O(n)的时间，不能限定算法输入，因此不能假设原数组已经有序

3. **Q12 在线性表a~0~, a~1~, ··· , a~100~中，删除元素a~50~需要移动()个元素**

错解：B. 50

正解：D. 0或50

注意没说是顺序表还是链表，如果是链表的话，无需移动元素即可完成删除

4. **Q21 对于一个带头结点的循环单链表L，判断该表为空表的条件是()**

错解：D. 头结点的指针域与L的地址相等

正解：C. 头结点的指针域与L的值相等

当带头结点的循环单链表L为空表时，满足L->next==L，就是比较值，而不是比对地址！

**指针的值是结点的地址，指针的地址没有意义！**

5. **Q22 对于一个带头结点的双循环链表L，判断该表为空表的条件是：()**

错解：C. L->prior\==NULL&&L->next\==L

正解：D. L->prior\==L&&L->next\==L

注意审题，这不是双向链表，这是双循环链表，不存在空指针，你个猪鼻

6. **Q23 一个链表最常用的操作是在末尾插入结点和删除结点，则选用()最节省时间**

错解：C. 带尾指针的单循环链表

正解：A. 带头结点的双循环链表

审题啊猪鼻，人家问的是删除最后的结点，带尾指针的单循环链表删除尾指针所在的结点还要兜一大圈

7. **Q25 设有两个长度为n的循环单链表，若要求两个循环单链表的头尾相接时间复杂度为O(1)，则对应两个循环单链表各设置一个指针，分别指向()**

错解：A. 各自的头结点

正解：B. 各自的尾结点

注意这里的重点是头尾相接，但是不知道人家玩的是bq还是pq还是bd，所以要把这个辅助指针设在尾结点，如果要用到头结点就移一步就可以了！

8. **Q30 分配较大空间，插入和删除元素不需要移动元素的线性表，其存储结构为()**

错解：D. 双链表

正解：B. 静态链表

注意这里说的是较大空间，大概率是连续的(不确定题目是不是漏了)，较大空间那就静态链表了！

## 3.1 栈

1. **Q1 栈和队列拥有相同的()**

错解：C.存储结构

正解：B.逻辑结构

我这里把"线性表"的概念误解成存储结构了，实际上应该是逻辑结构。(即线性结构)

它们的存储结构区别主要体现在一个FIFO，一个LIFO

2. **Q15 4个元素依次进栈的顺序是$a,b,c,d$，则以$c,d$开头的出栈序列个数为()**

错解：B. 2

正解：A. 1

不知道在想什么...a绝对不可能先于b出，所以只有一种序列

3. **Q29 一个栈的入栈序列为$1,2,3,...,n$，出栈序列是$P_1,P_2,P_3,...,P_n$，若$P_2=3$，则$P_3$的可能取值的个数是()**

错解：B. n-2

正解：C. n-1

我这里误解成$P_1$不可能是1了，因此选了B，实际上是有可能的

纯粗心。。。

## 3.2 队列
1. **Q8 假设用数组Q[MaxSize]实现循环队列，队首指针front指向队首元素的前一位置，队尾指针rear指向队尾元素，则判断该队列为空的条件是()**

错解：C. (Q.rear+1)%MaxSize==Q.front

正解：D. Q.rear==Q.front

什么自爆兵，注意看这题是判断队列为空，不是判断队列已满

2. **Q11 与顺序队列相比，链式队列()**

错解：A. 优点是队列的长度不受限制

正解：D. 缺点是不能根据队首指针和队尾指针计算队列的长度

注意虽然链式队列可以随时增长存储空间，但是其本身一样受内存空间的限制

3. **Q15 用链式存储方式的队列进行删除操作时需要()**

错解：A. 仅修改头指针

正解：D. 头尾指针都可能需要修改

当队列只剩一个元素时，这时候只修改头指针会导致尾指针变为游离指针，意味着尾指针也需要进行修改，使其指向front

4. **Q16 在一个链队列中，假设队头指针为front，队尾指针为rear，x所指向的元素需要入队，则需要执行的操作为()**

错解：C. rear->next=x, rear=x

正解：D. rear->next=x, x->next=NULL, rear=x

C的做法不够严密，因为x是队尾元素，所以x->next必须置空

5. **Q17 假设循环单链表表示的队列长度为n，队头固定在链表尾，若只设头指针，则进队操作的时间复杂度为()**

错解：B. $O(1)$

正解：A. $O(n)$

对定义不清晰了，进队实际上是在链表尾，所以每一次都要遍历一次；选错了是因为记成进队在链表头进行了，千万别这么想

6. **Q19 若以1,2,3,4作为双端队列的输入序列，则既不能由输入受限是双端队列得到，又不能由输出受限的双端队列得到的输出序列是：**

错解：D. 4,2,1,3

正解：C. 4,2,3,1

自己比划比划就知道了...无论如何3总是不能在第三个出来

## 3.4 数组与特殊矩阵

1. **Q4 在二维数组A中，假设每个数组元素的长度为3个存储单元，行下标i为0\~8，列下标j为0\~9，从首地址SA开始连续存放，在这种情况下，元素A[8][5]的起始地址为：**

错选： C. SA+222

正选： D. SA+255

算的时候注意到前面的行下标是从0开始了，但是加上这一列的下标的时候忘了要多算上0...

实际上应该是SA+((10\*8)+5)*3=SA+255

在写这条式子的时候我又忘了算上前面行和列的起始0了。。这个一定要多小心啊。。。

2. **Q7 若将n阶上三角矩阵A按列优先级压缩存放在一维数组B[1..n(n+1)/2+1]中，则存放到B[k]中的非零元素$a_{i,j}$的下标i,j与k对应的关系是：**

错选：D. $j(j-1)/2+i-1$

正选：C. $j(j-1)/2+i$

byd，注意B的下标是从1开始，又被坑了吧

3. **Q8 若将n阶下三角矩阵A按列优先顺序压缩存放在一维数组B[1..n(n+1)/2+1]中，则存放到B[k]中的非零元素$a_{i,j}$的下标i,j与k对应的关系是：**

错选：A. $(j-1)(2n-j+1)/2+i-j$

正选：B. $(j-1)(2n-j+2)/2+i-j+1$

下标还是从1开始...我服了。。。

4. **Q10 下列关于矩阵的说法中，正确的是：**

错解：II. 稀疏矩阵的特点是矩阵中的元素较少

正解：无正确项

注意，稀疏矩阵的特点是矩阵中的<u>非零元</u>个数较少，而不是元素较少

5. **Q11 有一个100阶的三对角矩阵M，其元素$m_{i,j}$按行优先依次压缩存入下标从0开始的一维数组N中。元素$m_{30,30}$在N中的下标是：**

错解：D. 89

正解：B. 87

(28*3)+2(首行)+1(该元素前面一个)=87

选错了可能是因为把元素本身算上了，以及首行算成了3

6. **Q13 设有一个12\*12阶对称矩阵M，将其上三角部分的元素$m_{i,j}$按列优先存入C语言的一维数组N中，元素$m_{6,6}$在N中的下标是**

错解：C. 55

正解：A. 50

错解是因为算对了前面的行，但是忘记了这是上三角矩阵，所以$m_{6,6}$实际上是这一行第一个存入的元素...而不是第6个

7. **Q14 将一个10\*10阶对称矩阵M的上三角部分的元素$m_{i,j}$按列优先存入C语言的一维数组N中，元素$m_{7,2}$在N中的下标是**

错解：B. 16

正解：C. 22

注意是按列优先，不是按行优先...这里看成按行优先了所以算错了

这个元素前面有(1+6)\*6/2=21个元素，那么$m_{7,2}$是第23个元素，在数组中的下标是22

## 5.2 二叉树的概念

1. **Q03 以下说法中，正确的是：**

错解：D. 结点按完全二叉树层序编号的二叉树中，第i个结点的左孩子编号为2i

正解：A. 在完全二叉树中，叶结点的双亲的左兄弟(若存在)一定不是叶结点

注意左孩子不一定存在...

A是对的，仔细一想还真是，是说叶结点->双亲->左兄弟，我误解成叶结点的左兄弟了，注意审题！


2. **Q08 设二叉树有2n个结点，且m\<n，则不可能存在( )的结点。**

错解：D. 2m个度为2

正解：C. 2m个度为1

3. **Q16 一棵有124个叶结点的完全二叉树，最多有()个结点**

错解：A. 247

正解：B. 248

4. **Q18 设有n个结点的二叉树采用三叉链表表示，其中每个结点包含3个指针，分别指向其左孩子、右孩子及父节点，则下列说法正确的是：()**

错解： III. 每个叶结点均被一个指针所指向

正解： 该选项不正确

5. **Q24 已知一棵完全二叉树的第6层(设根节点为第1层)有8个叶结点，则该完全二叉树的结点个数<u>最多</u>是()**

错解：A. 39

正解：C. 111

6. **Q27 对于<u>任意</u>一棵高度为5且有10个结点的二叉树，若采用顺序存储结构保存，每个结点占1个存储单元，则存放该二叉树需要的存储单元数量<u>最少</u>是()**

错解：B. 16

正解：A. 31

## 5.3 二叉树的遍历与线索二叉树

1. **Q05 在二叉树中有两个结点m与n，若m是n的祖先，则使用()可以找到从m到n的路径**

错解：A. 先序遍历

正解：C. 后序遍历

2. **Q21 某二叉树采用二叉链表存储结构，若要删除该二叉链表中的所有结点，并释放它们占用的存储空间，则采用()遍历方法最合适**

错解：B. 层次

正解：C. 后序

3. **Q23 线索二叉树是一种()结构**

错解：B. 逻辑与结构

正解：C. 物理
























